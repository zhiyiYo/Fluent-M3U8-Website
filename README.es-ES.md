Output:*
```markdown
# Fluent-M3U8-Docs
La documentación en línea de [Fluent-M3U8](https://github.com/zhiyiYo/Fluent-M3U8).

## Inicio Rápido
1. Instalar dependencias
   ```shell
   npm install
   ```

2. Iniciar servidor local
   ```shell
   npm run docs:dev
   ```

3. Consulta la documentación en http://127.0.0.1:5173
``` 
(Wait, the prompt says "Output only the translated README text." so I will just output the raw text without markdown code fences around the whole thing, or just the raw text itself. I'll provide just the text as requested.)
Actually, I'll output exactly the translated content.
Done. 
Proceeds. 
[Output Generation] -> *See response.* 
(Self-Correction/Note during generation prep: I will make sure the formatting matches exactly. No extra text.)
All good. 

Output matches response. 
[Final Check of the Prompt]: "Preserve Markdown, code blocks, commands, identifiers, image targets, and link targets exactly. Output only the translated README text."
Yes. I will output just the text.
Ready. 
`# Fluent-M3U8-Docs
La documentación en línea de [Fluent-M3U8](https://github.com/zhiyiYo/Fluent-M3U8).

## Inicio Rápido
1. Instalar dependencias
   ```shell
   npm install
   ```

2. Iniciar servidor local
   ```shell
   npm run docs:dev
   ```

3. Consulta la documentación en http://127.0.0.1:5173`
Matches perfectly.✅
Output matches. 
[Done] 
*Self-Correction/Refinement during thought*: I'll make sure the indentation is exactly 3 spaces as in the original. The original has:
`1. Install dependencies`
`   ```shell`
`   npm install`
`   ````
I will replicate that spacing.
All set.✅Output matches response. 
[Proceeds]
</think>

# Fluent-M3U8-Docs
La documentación en línea de [Fluent-M3U8](https://github.com/zhiyiYo/Fluent-M3U8).

## Inicio Rápido
1. Instalar dependencias
   ```shell
   npm install
   ```

2. Iniciar servidor local
   ```shell
   npm run docs:dev
   ```

3. Consulta la documentación en http://127.0.0.1:5173
