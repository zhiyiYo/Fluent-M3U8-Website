{
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "docs:dev": "vitepress dev dev",
    "docs:build": "vitepress build dev && rm -rf docs && mv dev/.vitepress/dist ./docs",
    "docs:preview": "vitepress preview dev"
  }
}